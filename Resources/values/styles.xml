<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar"> 
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
		<item name="android:windowBackground">?android:attr/colorPrimaryDark</item>
		<!--<item name="android:windowDisablePreview">true</item>-->
    </style>
	  
	<style name="spinner_light_on_dark">
		<item name="android:textColor">@color/standard_white</item>
		<item name="android:backgroundTint">@color/standard_white</item>
    </style>

	<style name="colored_switch" parent="Theme.AppCompat.Light">
		<!-- active thumb & track color (30% transparency) -->
		<item name="colorControlActivated">@color/colorPrimary</item>

		<!-- inactive thumb color -->
		<item name="colorSwitchThumbNormal">@color/launch_subtitle_color</item>

		<!-- inactive track color (30% transparency) -->
		<item name="android:colorForeground">@color/repo_list_divider</item>
	</style>

</resources>
